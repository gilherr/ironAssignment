// const { Application } = require('../applications')
// const logger = require('../../loaders/logger')

async function updateApplication (category) {
  // const apps = await Application.find({ category }, 'name avgAge')
  // return apps
}

module.exports = {
  updateApplication
}
